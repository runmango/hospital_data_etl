# LIS SQL Notes

LIS business table names are not confirmed yet. The first version only performs metadata discovery:

- owners
- accessible tables
- keyword-matched candidate tables
- keyword-matched candidate columns
- explicitly requested table schema
- small, rownum-limited table samples

Do not add fixed LIS result/report extraction SQL until the hospital confirms table names and patient/result field mappings.
